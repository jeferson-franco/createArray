function solution(size) {
    return Array(size).fill(1);
}

module.exports = solution;

// npm init -y
// npm i jest-cli -g
// package.json: "scripts": { "test": "jest" }
// npm test createArray

// alternative solution
